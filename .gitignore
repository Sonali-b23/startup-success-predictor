__pycache__/
*.pyc
.pytest_cache/
venv/
.venv/

# Trained models are regenerated by `python runner.py` -- not committed so
# the repo doesn't silently ship a stale model that no longer matches the
# code that produced it.
models/
